<h1>Mails#index</h1>
<p>Find me in app/views/mails/index.html.erb</p>
 <%=link_to"New", emails_path, method: :post, remote: true, class:"btn btn-primary"%>

<div class="container">

  <div class="row">

    <div class="col-sm">
    <% @emails.each do |email| %>
      <ul class="list-group">
    <li class="list-group-item"><%= email.object%></li>
      </ul>
<% end %>
    </div>
    <div class="col-sm">
    <% @emails.each do |email| %>
      <ul class="list-group">
    <li class="list-group-item"><%= email.body%></li>
      </ul>
<% end %>
    </div>
  </div>
</div>

